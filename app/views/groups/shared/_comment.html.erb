 <%= form_for([@groupTodo,@comment],{:action => "create", :controller => "comments", :group_todo_id => todo.id}) do |f| %>
    <%= f.text_field :text, id: :text %>
    <%= f.hidden_field :group_id, :value => @group_id %>
    <%= f.hidden_field :user_id, :value => current_user.id %>
    <%= f.submit "コメント"%>
<% end %>
